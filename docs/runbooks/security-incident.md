# 安全事件

## API Key 泄露

立即禁用并轮换 Key，更新服务器环境变量，重启相关服务，检查审计和 provider 调用记录。不得在工单、日志或仓库中粘贴密钥。

## 客户数据疑似泄露

冻结可疑访问凭证，保留审计证据，确认组织边界、文件路径和下载记录，通知负责人并按隐私流程处理。不要通过平台后台默认打开客户报告、上传文件或私有知识库正文。

## 恶意文件

隔离文件、停止解析/导出任务并保留 hash。当前没有 ClamAV 集成，这是生产上线前的已知风险；存储目录不得作为可执行静态目录。

